#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int solution(int n) {
    int answer = 2;

    while (answer<n)
    {
        if (n%answer == 1) return answer;
        answer++;
    }
}

int main()
{
    int a=0;
    scanf("%d",&a);
    printf("%d",solution(a));
}
