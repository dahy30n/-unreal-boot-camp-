#include <stdio.h>
#include <stdbool.h>

bool solution(int x) {
    int n = x, sum = 0;
    while (n > 0) {
        sum += n % 10;
        n /= 10;
    }
    if ((x%sum)==0) return true;
    else return false;

}

int main() {
    int a;
    scanf("%d", &a);
    printf("%d\n", solution(a));
}
